const config = {
  siteUrl: process.env.NEXT_PUBLIC_BASE_URL || "https://codertechltd.com",
  generateRobotsTxt: true,
  sitemapSize: 7000,
  changefreq: "daily",
  priority: 0.7,
};

export default config;
